// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef XHLS_CONTRAST_STRETCH_H
#define XHLS_CONTRAST_STRETCH_H

#ifdef __cplusplus
extern "C" {
#endif

/***************************** Include Files *********************************/
#ifndef __linux__
#include "xil_types.h"
#include "xil_assert.h"
#include "xstatus.h"
#include "xil_io.h"
#else
#include <stdint.h>
#include <assert.h>
#include <dirent.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>
#include <unistd.h>
#include <stddef.h>
#endif
#include "xhls_contrast_stretch_hw.h"

/**************************** Type Definitions ******************************/
#ifdef __linux__
typedef uint8_t u8;
typedef uint16_t u16;
typedef uint32_t u32;
#else
typedef struct {
    u16 DeviceId;
    u32 Axilites_BaseAddress;
} XHls_contrast_stretch_Config;
#endif

typedef struct {
    u32 Axilites_BaseAddress;
    u32 IsReady;
} XHls_contrast_stretch;

/***************** Macros (Inline Functions) Definitions *********************/
#ifndef __linux__
#define XHls_contrast_stretch_WriteReg(BaseAddress, RegOffset, Data) \
    Xil_Out32((BaseAddress) + (RegOffset), (u32)(Data))
#define XHls_contrast_stretch_ReadReg(BaseAddress, RegOffset) \
    Xil_In32((BaseAddress) + (RegOffset))
#else
#define XHls_contrast_stretch_WriteReg(BaseAddress, RegOffset, Data) \
    *(volatile u32*)((BaseAddress) + (RegOffset)) = (u32)(Data)
#define XHls_contrast_stretch_ReadReg(BaseAddress, RegOffset) \
    *(volatile u32*)((BaseAddress) + (RegOffset))

#define Xil_AssertVoid(expr)    assert(expr)
#define Xil_AssertNonvoid(expr) assert(expr)

#define XST_SUCCESS             0
#define XST_DEVICE_NOT_FOUND    2
#define XST_OPEN_DEVICE_FAILED  3
#define XIL_COMPONENT_IS_READY  1
#endif

/************************** Function Prototypes *****************************/
#ifndef __linux__
int XHls_contrast_stretch_Initialize(XHls_contrast_stretch *InstancePtr, u16 DeviceId);
XHls_contrast_stretch_Config* XHls_contrast_stretch_LookupConfig(u16 DeviceId);
int XHls_contrast_stretch_CfgInitialize(XHls_contrast_stretch *InstancePtr, XHls_contrast_stretch_Config *ConfigPtr);
#else
int XHls_contrast_stretch_Initialize(XHls_contrast_stretch *InstancePtr, const char* InstanceName);
int XHls_contrast_stretch_Release(XHls_contrast_stretch *InstancePtr);
#endif


void XHls_contrast_stretch_Set_height(XHls_contrast_stretch *InstancePtr, u32 Data);
u32 XHls_contrast_stretch_Get_height(XHls_contrast_stretch *InstancePtr);
void XHls_contrast_stretch_Set_width(XHls_contrast_stretch *InstancePtr, u32 Data);
u32 XHls_contrast_stretch_Get_width(XHls_contrast_stretch *InstancePtr);
void XHls_contrast_stretch_Set_min(XHls_contrast_stretch *InstancePtr, u32 Data);
u32 XHls_contrast_stretch_Get_min(XHls_contrast_stretch *InstancePtr);
void XHls_contrast_stretch_Set_max(XHls_contrast_stretch *InstancePtr, u32 Data);
u32 XHls_contrast_stretch_Get_max(XHls_contrast_stretch *InstancePtr);

#ifdef __cplusplus
}
#endif

#endif
